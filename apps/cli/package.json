{
  "name": "@panda/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "panda": "./dist/index.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json && node scripts/make-bin-executable.mjs",
    "panda": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm typecheck"
  },
  "dependencies": {
    "@panda/sdk": "workspace:*",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2"
  }
}
